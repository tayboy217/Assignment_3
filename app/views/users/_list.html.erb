
   <div class='container'>
   <div class='row'>
    <div class='col-md-3'>
    <h2>User info</h2>
     <table class='table'>
    <tr><%= attachment_image_tag(users, :image, fallback: "no-image.png", size:"100x100") %></tr>
    <%= form_with model:users, local:true do |f| %>
    <th><%= f.label :name, "Name", class: "label" %></th>
    <th><%= users.name %></th>
    </tr>
    <th><%= f.label :introduction, "Introduction", class: "label" %></th>
    <th><%= users.introduction %></th>
    </tr>
    <% end %>
    </table>
    <div class="row">
    <%= link_to edit_user_path(users), class: "btn btn-outline-secondary btn-block" do %>
    <i class="fas fa-user-cog"></i>
    <% end %>
    </div>

 <h2 class="mt-3">New book</h2>
　 <%= form_with model: book, local:true do |f| %>
   <div class="form-group">
   <%= f.label :title, class: "label" %>
   <%= f.text_field :title, class: "form-control book_title" %>
   </div>
   <div class="form-group">
   <%= f.label :body, class: "label" %>
   <%= f.text_field :body, class: "form-control book_body" %>
   </div>
   <div class="form-group">
   <%= f.submit "submit", class: "btn btn-success" %>
    </div>
    </div>
    <% end %>


